/vendor/
/composer.lock

/.env
/.env.*
!/.env.example

/.phpunit.cache/
/.phpunit.result.cache
/coverage/

/node_modules/

/.idea/
/.vscode/
*.sublime-*

*.log
.DS_Store
Thumbs.db

# Agent tooling: keep the config, never the worktrees.
# A worktree is a live checkout with its own .git pointer; committing it
# records a submodule-style gitlink that breaks for everyone who clones.
.blkdem/worktrees/

